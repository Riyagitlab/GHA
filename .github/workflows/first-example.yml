name: my first workflow

on: push

jobs: 
  
  first_job:
    runs-on: ubuntu-latest
    steps:
        - name: checkout Repo
          uses: actions/checkout@v7

        - name: list and read files
          run: |
               echo "Hi" 
               ls -ltra

        - name: Generate ASCII artwork
          run: cowsay -f elephant "Run for cover,I am a elephant.. RAWR" >> elephant.txt       

               

       































































